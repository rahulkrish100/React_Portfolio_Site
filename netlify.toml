[build]
  command = "npm run build"
  publish = "dist"

# Redirect all routes to index.html so React Router's client-side
# routes (e.g. /about, /projects) work correctly on refresh/deep-link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
